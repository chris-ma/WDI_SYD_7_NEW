<%= form_for(@todotask) do |f| %>
  <% if @todotask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todotask.errors.count, "error") %> prohibited this todotask from being saved:</h2>

      <ul>
      <% @todotask.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="field">
    <%= f.label :completed %><br>
    <%= f.check_box :completed %>
  </div>
 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
